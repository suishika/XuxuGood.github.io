/* 主页logo */
.cover-wrapper .cover-body .logo {
  width: 700px;
  max-height: 130px;
}

/* 解决定位valine评论位置问题 */
.l_main .post {
  transform: none !important;
}

/* 不设置鼠标样式的在这里处理 */
.article.article-entry.l_friends .friends-group .friend-content .friend-card .friend-right p.friend-name,
.article.article-entry.l_friends .friends-group .friend-content .friend-card .friend-right p.tags {
  cursor: unset;
}

/*自定义右键菜单按钮与下拉菜单按钮样式*/
.article .article-entry .dropmenu ul li:before {
  content: none !important;
}

.rightmenu hr {
  border-bottom: 1px solid alpha($color-text, .1) !important
}

ul.list-v.rightmenu, .dropmenu-wrapper .dropmenu ul.list-v {
  background: $color-card;
  box-shadow: $boxshadow-dropmenu;
}

ul.list-v.rightmenu li a, .dropmenu-wrapper .dropmenu ul.list-v > li > a {
  border-left: none;
  color: mix($color-text, $color-card, 80);
  padding: 0 $gap + $border-line + 2px 0 $gap;
  margin: 0 4px;
  border-radius: 4px;
}

ul.list-v.rightmenu li a:hover, .dropmenu-wrapper .dropmenu ul.list-v > li > a:hover {
  color: mix($color-theme, $color-text, 70);
  background: bgcolor($color-theme);
}

/* 时间线样式 */
div.timenode .body {
  font-size: $fontsize-list
}

/* 归档页样式优化 */
#archive-page {
  padding-bottom: 32px;
}

#archive-page .archive .archive-year h2 {
  margin-top: 0;
}

#archive-page .archive .archive-post a:hover {
  padding-top: 8px;
  padding-bottom: 8px;
}

#archive-page div.timenode:before {
  top: 2px;
}

#archive-page div.timenode:after {
  top: 28px;
}

#archive-page div.timenode .meta:before, #archive-page div.timenode .meta:after {
  top: 10px;
}

/* 公告中使用的 kbd 样式 */
.widget.text .content kbd {
  cursor: default;
  display: inline-block;
  padding: 3px 5px;
  font: 11px SFMono-Regular, Consolas, Liberation Mono, Menlo, monospace;
  line-height: 10px;
  vertical-align: 0.05rem;
  border: 1px solid #d1d5da;
  border-radius: 6px;
}

/* 自定义文章标题样式/解决文章标题多次点击事件 */
.l_main .post .article-header .title a {
  pointer-events: none;
}

.l_main .post .article-meta#top {
  margin-top: 0;
  margin-bottom: 0;
}

/* 自定义文章footer hr样式 */
.l_main .post .article-meta#bottom hr {
  margin-bottom: 10px;
  position: relative;
  border: 2px dashed alpha($color-theme, .5);
  overflow: visible;
}

.l_main .post .article-meta#bottom hr:hover:before {
  left: 95%;
}

.l_main .post .article-meta#bottom hr:before {
  position: absolute;
  top: -10px;
  left: 5%;
  color: $color-theme;
  content: "\f0c4";
  font-family: "Font Awesome 5 Free";
  font-weight: 900;
  font-size: 1.125rem;
  -webkit-transition: all 1s ease-in-out;
  -moz-transition: all 1s ease-in-out;
  -o-transition: all 1s ease-in-out;
  -ms-transition: all 1s ease-in-out;
  transition: all 1s ease-in-out;
}

/* 文章h标签距离顶部样式 */
.article .article-entry > h1:before, .article .article-entry > h2:before {
  margin-top: -60px;
}

.article .article-entry > h3:before,
.article .article-entry > h4:before {
  margin-top: -70px;
}

/* 阅读全文按钮样式 */
.meta-v3 .readmore {
  color: $color-theme;
}

.meta-v3 .readmore:hover {
  color: alpha($color-theme, .7);
}

/* 文章样式 */
.article a {
  word-break: break-all;
}

/* 文章翻页样式 */
.l_main .article .prev-next > a:hover {
  background: bgcolor($color-theme)
}

.l_main .article .prev-next > a:hover p.title {
  color: $color-theme;
}

/* 打字机效果 */
.typed-cursor {
  animation: typedjsBlink 0.7s infinite;
  -webkit-animation: typedjsBlink 0.7s infinite;
}

@keyframes typedjsBlink {
  50% {
    opacity: 0.0;
  }
}

@-webkit-keyframes typedjsBlink {
  0% {
    opacity: 1;
  }
  50% {
    opacity: 0.0;
  }
  100% {
    opacity: 1;
  }
}

/* 首页cover menu样式 */
/* search版块 */
.cover-wrapper.search .bottom .menu .list-h a:hover, .cover-wrapper.search .bottom .menu .list-h a.active, .cover-wrapper.search .bottom .menu .list-h a:active {
  color: $color-theme;
  border-bottom: 2px solid $color-theme;
}

/* 首页cover input样式 */
.cover-wrapper .cover-body .m_search .input {
  display: block;
  font-size: 16px;
  line-height: 16px;
  height: 44px;
  width: 100%;
  color: $color-theme;
  box-shadow: none;
  box-sizing: border-box;
  -webkit-appearance: none;
  padding-left: 36px;
  border-radius: 56px;
  background: alpha($color-theme, .1);
  border: 1px solid $color-theme;
}

.cover-wrapper .cover-body .m_search .input:focus {
  border: 3px solid $color-theme;
  background: alpha($color-theme, .1);
}

.cover-wrapper .cover-body .m_search .input:hover {
  background: alpha($color-theme, .1);
}

.cover-wrapper .cover-body .m_search .input:hover ~ .icon {
  color: $color-theme;
}

.cover-wrapper .cover-body .m_search .icon {
  line-height: 2.75rem;
}

/* 背景代码雨样式 */
#codeRainCanvas {
  position: fixed;
  right: 0;
  bottom: 0;
  min-width: 100%;
  min-height: 100%;
  height: auto;
  width: auto;
  z-index: -1;
}

/* 首页向下滚动图标颜色 */
.cover-wrapper .scroll-down .scroll-down-effects {
  color: $color-scroll-down;
}

/* 向上滚动图标颜色 */
.l_body .s-top {
  color: $color-scroll-up;
}

/* blogger 小组件title样式 */
.widget.blogger .content .text :first-child {
  font-size: 1.125rem;
}

/* footer 样式*/
footer.footer {
  color: $color-card;
}

footer.footer .social-wrapper a {
  padding: 0;
  color: $color-card !important;
}

footer.footer a.social:hover {
  background: $color-theme;
}

footer.footer a {
  color: $color-text;
}

footer.footer a:hover {
  text-decoration: none !important;
  color: $color-hover
}

.github-badge {
  margin-left: 5px;
  display: inline-block;
  border-radius: 4px;
  text-shadow: none;
  color: #fff;
  line-height: 15px;
  background-color: #ABBAC3;
  margin-bottom: 5px;
}

.github-badge .badge-subject {
  display: inline-block;
  background-color: #4D4D4D;
  padding: 4px 4px 4px 6px;
  border-top-left-radius: 4px;
  border-bottom-left-radius: 4px;
}

.github-badge .badge-value {
  display: inline-block;
  padding: 4px 6px 4px 4px;
  border-top-right-radius: 4px;
  border-bottom-right-radius: 4px;
}

.github-badge .bg-blue {
  background-color: #007EC6;
}

.github-badge .bg-brightgreen {
  background-color: #4DC820;
}

.github-badge .bg-blueviolet {
  background-color: #8833D7;
}

.github-badge .bg-orange {
  background-color: #FFA500;
}

.github-badge .bg-blue {
  background-color: #007EC6;
}

.github-badge .bg-lightgray {
  background-color: #9f9f9f;
}

.github-badge .bg-gradient {
  background: linear-gradient(to right, #3ca5f6, #a86af9);
}

/* 标签页样式 */
#archive-page .archive .all-tags ul li:hover {
  background: $color-block;
  box-shadow: 0 5px 15px 0 rgba(0, 0, 0, .4);
}

#archive-page .archive .all-tags ul li:hover a {
  color: $color-text !important;
}

#archive-page .archive .all-tags ul li:hover span {
  background: darken($color-block, 5)
}

/* 导航栏样式 */
.l_header {
  background: $color-theme !important;
}

.l_header .wrapper .nav-main .title {
  color: $color-card;
  font-weight: bold;
  font-size: 1.5625rem !important;
}

.l_header .wrapper .nav-sub .title {
  color: $color-card;
  font-weight: bold;
}

.l_header ul > li > a {
  color: $color-card !important;
}

.l_header ul > li > a.active {
  color: $color-card;
  border-left-color: $color-card;
  border-bottom-color: $color-card;
}

.l_header ul > li > a:hover {
  color: $color-card;
  border-left-color: $color-card;
  border-bottom-color: $color-card;
  background: alpha($color-card, .2);
}

.l_header ul.list-v {
  background: $color-theme;
}

.l_header ul.list-v hr {
  border-bottom: 0.5px solid #fff;
}

.l_header .m_search {
  width: 200px;
}

.l_header .m_search .input {
  border: 1px dashed transparent;
  color: $color-card;
  background: alpha($color-card, .2);
}

.l_header .m_search .input:focus {
  border: 2px solid $color-card;
}

.l_header .m_search .icon {
  color: alpha($color-card, .8);
}

.l_header .m_search .input::-webkit-input-placeholder {
  color: $color-card;
}

.l_header .m_search .input:-moz-placeholder {
  color: $color-card;
}

.l_header .m_search .input::-moz-placeholder {
  color: $color-card;
}

.l_header .m_search .input:-ms-input-placeholder {
  color: $color-card;
}

/* 主页cover打字机字体颜色 */
if hexo-config('coverTypeWriter.enable') {
  #subtitle-style {
    color: $color-theme
  }
}

/* 小组件header样式 */
if hexo-config('widgetHeader.enable') {
  .widget header.pure {
    background-color: $color-theme;
    color: $color-card;
    padding-top: 8px;
    padding-bottom: 8px;
  }

  .widget header {
    display: flex;
    justify-content: space-between;
    line-height: 1.5em;
  }

  .widget header.pure .rightBtn {
    color: $color-card;
  }

  .widget header.pure .rightBtn:hover {
    color: $color-hover;
  }
}

/* 有序图标设置 */
.article .article-entry ol {
  counter-reset: sectioncounter
}

.article .article-entry ol > li {
  margin-top: .4rem;
  list-style: none;
  line-height: 30px;
}

.article .article-entry ol > li:before {
  display: block;
  float: left;
  width: 17px;
  height: 17px;
  line-height: 16px;
  margin: 0.42rem 0.42rem 0 0;
  color: $color-card;
  font-size: 15px;
  font-weight: 500;
  font-style: normal;
  background-color: $color-theme;
  border-radius: 50%;
  text-align: center;
  content: '' counter(sectioncounter) '';
  counter-increment: sectioncounter;
  transition: all .5s;
}

.article .article-entry ol li:hover::before {
  transform: rotate(360deg);
  -webkit-transform: rotate(360deg);
  -moz-transform: rotate(360deg);
  -o-transform: rotate(360deg);
  -ms-transform: rotate(360deg);
}

/* 无序图标设置 */
.article .article-entry ul li {
  list-style: none;
}

.article .article-entry ul li:before {
  float: left;
  left: 0;
  margin: 0.3rem 0.42rem 0 0;
  width: 8px;
  height: 8px;
  border: .2rem solid $color-theme;
  border-radius: 50%;
  background: $color-card;
  content: "";
  line-height: 19px;
  -webkit-transition: all .3s ease-out;
  -moz-transition: all .3s ease-out;
  -o-transition: all .3s ease-out;
  -ms-transition: all .3s ease-out;
  transition: all .3s ease-out;
}

.article .article-entry ul > li:hover:before {
  border-color: $color-hover;
}


.article .article-entry div.tabs ul.nav-tabs li:before {
  content: none;
}

/* 微信分享二维码层级 */
div.hoverbox:hover > div.target {
  z-index: 2;
}

/*valine 评论系统样式 began */

.v .vcards .vcard .vimg {
  transition: all ease 1s;
}

.v .vcards .vcard .vimg:hover {
  transform: rotate(360deg);
}

.l_main #comments #valine_container .vcards .vmeta .vat {
  color: $color-theme;
}

.l_main #comments #valine_container .vcards .vmeta .vat:hover {
  color: $color-theme;
  text-decoration: none;
}

.l_main #comments #valine_container .vcards .vcontent code {
  vertical-align: 0.1em;
}

.l_main #comments #valine_container .vcards .vhead span.vnick {
  vertical-align: -0.12em;
}

.l_main #comments #valine_container .vcards .vcontent a:hover {
  color: #ff5722;
  text-decoration: none;
}

.l_main #comments #valine_container button:hover {
  color: $color-theme;
  background: bgcolor($color-theme)
}

.v .vcards .vcard .vh .vmeta .vat {
  padding: 0 .8rem !important;
  border: 1px solid $color-theme !important;
  border-radius: 5px !important;
}

.v .vcards .vcard .vh .vmeta .vat:hover {
  background: bgcolor($color-theme)
}

.l_main #comments #valine_container .vcards .vh {
  border-bottom: none;
}

.l_main #comments #valine_container .vcards .vhead .vsys {
  padding: 0 8px 1px 8px;
  margin: 0;
}

.l_main #comments #valine_container .vcards .vhead .vsys > i {
  margin-right: 5px;
}

.l_main #comments #valine_container .vcards .vcontent a {
  display: contents;
}

#valine_container .vcards .vcard {
  padding: 15px 20px 0 20px;
  border-radius: 10px;
  margin-bottom: 15px;
  border: 1px dashed alpha($color-text, .3)
  transition: all .3s;
}

#valine_container .vcards .vcard:hover {
  border: 1px dashed $color-theme;
}

#valine_container .vcards .vcard .vh {
  padding-bottom: 0;
  border-bottom: none;
}

#valine_container .vcards .vcard .vh .vcard {
  padding-left: 0;
  padding-right: 0;
  border: none;
  box-shadow: none;
}

#valine_container .vcards .vcard h1,
#valine_container .vcards .vcard h2,
#valine_container .vcards .vcard h3,
#valine_container .vcards .vcard h4,
#valine_container .vcards .vcard h5,
#valine_container .vcards .vcard h6 {
  border-bottom: none;
}

#veditor {
  //resize: none !important;
  background: url(https://cdn.jsdelivr.net/gh/XuxuGood/cdn@master/blogImages/valine/comment.webp) 100% 100% no-repeat;
}

#veditor:focus {
  background-position-y: 400px;
  transition: all 0.2s ease-in-out 0s;
}

/*valine 评论系统样式 end */

/* 导航切换switcher样式 */
.l_header .switcher > li > a.active, .l_header .switcher > li > a:active {
  background: alpha($color-card, .2);
}

/* 搜索弹窗样式 */
#u-search .modal-header #u-search-modal-form #u-search-modal-input {
  margin: 12px 50px;
}

//手机适配
@media screen and (max-width: 1024px) {
  .l_header .m_search {
    width: 44px;
  }
}

@media screen and (max-width: 500px) {
  /* 搜索框样式 */
  .l_header.z_search-open .m_search {
    width: calc(100vw - 2 * 20px - 2 * 32px);
  }

  .l_header .m_search {
    width: 0;
    overflow: hidden;
    position: absolute;
    background: none;
    transition: all 0.25s ease;
    -moz-transition: all 0.25s ease;
    -webkit-transition: all 0.25s ease;
    -o-transition: all 0.25s ease;
    margin: 0 10px;
  }

  .l_header .m_search .icon {
    display: block;
  }

  .l_header .m_search .input {
    margin-left: 0;
    font-size: 16px;
    width: calc(100% - 30px) !important;
    -webkit-appearance: none;
    padding-left: 36px;
    border-radius: 8px;
    border: 1px dashed transparent;
  }

  .l_header.z_search-open .title {
    opacity: 0;
  }
}

@media screen and (max-width: 768px) {
  .l_side .toc-wrapper {
    border: none;
  }

  #veditor {
    background: none;
  }
}
